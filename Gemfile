source "https://rubygems.org"

# Mirrors the exact Jekyll + plugin versions GitHub Pages runs, so local
# previews match production. Optional — GitHub Pages does not need this file.
gem "github-pages", group: :jekyll_plugins
